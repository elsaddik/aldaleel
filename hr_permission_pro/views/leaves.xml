<odoo>
    <record id="hr_leave_form_inherit_multi_approval_mangr" model="ir.ui.view">
        <field name="name">hr.leave.form.multi.approval.mangr</field>
        <field name="model">hr.leave</field>

        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form_manager"/>
        <field name="arch" type="xml">

            <xpath expr="//header/button[@name='action_approve']" position="replace">
                <button name="action_approve"
                        type="object"
                        string="Manager Approve"
                        class="btn-primary"
                        invisible=" state != 'manager_approve' "/>

                <button name="action_approve"
                        type="object"
                        string="HR Approve"
                        class="btn-success"
                        groups="hr_holidays.group_hr_holidays_user"
                        invisible="state != 'hr_approve'"/>
            </xpath>
        </field>
    </record>
    
    <record id="hr_leave_form_inherit_multi_approval" model="ir.ui.view">
        <field name="name">hr.leave.form.multi.approval</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">

                <button name="action_submit_for_approval"
                        type="object"
                        string="Submit for Approval"
                        class="btn-primary"
                        invisible="state != 'confirm'"/>

            </xpath>
        </field>
    </record>
</odoo>
